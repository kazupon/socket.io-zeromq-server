/**
 * import(s)
 */

var debug = require('debug')('socket.io-zeromq-server');


/**
 * export(s)
 */

module.exports = Server;

function Server (opts) {
  opts = opts || {};

  // TODO: implements !!
  
  return this;
}
